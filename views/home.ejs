<%- include('partials/header'); -%>

<!-- main content -->

<h1>Home</h1>
<p><%= home_start %></p>

<% console.log(posts) %>
<% posts.forEach(function(post) { %>
    <h1><%= post.title %></h1>
    <p><%= post.content.substring(0,100)%><span>...</span><a href="/posts/<%= post.title %>" >Read more</a></p>
    <% }); %>
<!-- footer -->

<%- include('partials/footer'); -%>